# Copia este archivo a .env y configura tus valores

# Puerto del servidor
PORT=3000

# URL del sitio WordPress en Plesk
WORDPRESS_URL=https://tu-sitio-wordpress.com
WORDPRESS_API_URL=https://tu-sitio-wordpress.com/wp-json/wp/v2

# Credenciales de WordPress API (Application Password o Username:Password en base64)
WORDPRESS_USERNAME=tu_usuario
WORDPRESS_APPLICATION_PASSWORD=tu_application_password
# O usar directamente:
# WORDPRESS_AUTH_HEADER=Basic base64_encoded_username:password

# Custom Post Type de eventos en WordPress
WORDPRESS_EVENT_POST_TYPE=evento

# Webhook secret para validar requests de WordPress
WEBHOOK_SECRET=tu_secret_webhook_muy_seguro

# JWT Configuration
JWT_SECRET=tu_jwt_secret_muy_seguro_cambiar_en_produccion
JWT_EXPIRES_IN=7d

# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/eventu_db
# O para MongoDB Atlas:
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/eventu_db

# API Configuration
API_BASE_URL=https://api.eventu.com
API_VERSION=v1
API_TIMEOUT=30000

# Socket.IO Configuration
SOCKET_IO_CORS_ORIGIN=*

# CORS Configuration (separar múltiples orígenes con comas)
CORS_ORIGIN=https://app.eventu.com,https://eventu.com,*

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Polling Configuration (si no se usan webhooks)
POLLING_ENABLED=true
POLLING_INTERVAL_MINUTES=5

# Logging
LOG_LEVEL=info
LOG_FILE=logs/app.log

# Environment
NODE_ENV=development



